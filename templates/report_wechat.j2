## 🎯 {{ report_date }} {{ labels.dashboard_title }}

> {{ results|length }} {{ labels.stock_unit }} | 🟢{{ labels.buy_label }}:{{ buy_count }} 🟡{{ labels.watch_label }}:{{ hold_count }} 🔴{{ labels.sell_label }}:{{ sell_count }}
{%- if market_status_line %}
{{ market_status_line }}
{%- endif %}
{%- if summary_only %}

**📊 {{ labels.summary_heading }}**
{% if plain_summary -%}
{%- set changed = enriched | selectattr('advice_changed') | list -%}
{%- set unchanged = enriched | rejectattr('advice_changed') | list -%}
{%- if changed %}
**⚠️ {{ labels.plain_changed_heading }}（{{ changed|length }}）**

{% for e in changed -%}
{{ e.signal_emoji }} **{{ e.stock_name }}({{ e.result.code }})：{{ e.localized_operation_advice }}**{% if e.previous_advice %}（{{ labels.plain_prev_label }}：{{ e.previous_advice }}）{% endif %}
{% if e.plain.get('reason') %}- {{ labels.plain_reason_label }}：{{ e.plain.get('reason') }}
{% endif -%}
{% if e.plain.get('action_now') %}- {{ labels.plain_action_label }}：{{ e.plain.get('action_now') }}
{% endif -%}
{% if e.plain.get('change_condition') %}- {{ labels.plain_condition_label }}：{{ e.plain.get('change_condition') }}
{% endif %}
{% endfor -%}
{%- endif %}
{%- if unchanged %}
**{{ labels.plain_unchanged_heading }}（{{ unchanged|length }}，{{ labels.plain_unchanged_note }}）**

{% for e in unchanged -%}
{{ e.signal_emoji }} **{{ e.stock_name }}({{ e.result.code }})：{{ e.localized_operation_advice }}**
{% if e.plain.get('reason') %}- {{ labels.plain_reason_label }}：{{ e.plain.get('reason') }}
{% endif -%}
{% if e.plain.get('action_now') %}- {{ labels.plain_action_label }}：{{ e.plain.get('action_now') }}
{% endif -%}
{% if e.plain.get('change_condition') %}- {{ labels.plain_condition_label }}：{{ e.plain.get('change_condition') }}
{% endif %}
{% endfor -%}
{% endif -%}
{% else -%}
{% for e in enriched -%}
{{ e.signal_emoji }} **{{ e.stock_name }}({{ e.result.code }})**: {{ e.localized_operation_advice }} | {{ labels.score_label }} {{ e.result.sentiment_score }} | {{ e.localized_trend_prediction }}
{% endfor -%}
{%- endif %}
{%- else %}
{% for e in enriched %}
{%- set result = e.result -%}
{%- set dashboard = result.dashboard if result.dashboard else {} -%}
{%- set core = dashboard.get('core_conclusion') or {} -%}
{%- set battle = dashboard.get('battle_plan') or {} -%}
{%- set intel = dashboard.get('intelligence') or {} %}
### {{ e.signal_emoji }} **{{ e.signal_text }}** | {{ e.stock_name }}({{ result.code }})
{%- set one_sentence = core.get('one_sentence', result.analysis_summary) if core else result.analysis_summary %}
{%- if one_sentence %}
📌 **{{ one_sentence }}**
{%- endif %}
{%- if intel.get('earnings_outlook') %}
📊 {{ labels.earnings_outlook_label }}: {{ intel.earnings_outlook[:200] }}
{%- endif %}
{%- if intel.get('sentiment_summary') %}
💭 {{ labels.sentiment_summary_label }}: {{ intel.sentiment_summary[:150] }}
{%- endif %}
{%- if intel.get('risk_alerts') %}

🚨 **{{ labels.risk_alerts_label }}**:
{% for risk in intel.risk_alerts[:3] -%}
   • {{ risk[:120] }}{{ '...' if risk|length > 120 else '' }}
{% endfor -%}
{%- endif %}
{%- if intel.get('positive_catalysts') %}

✨ **{{ labels.positive_catalysts_label }}**:
{% for cat in intel.positive_catalysts[:3] -%}
   • {{ cat[:120] }}{{ '...' if cat|length > 120 else '' }}
{% endfor -%}
{%- endif %}
{%- set phase_decision = dashboard.get('phase_decision') or {} %}
{%- if phase_decision.get('watch_conditions') %}

🛡️ **开盘应对**:
{% for condition in phase_decision.get('watch_conditions')[:4] -%}
   • {{ condition[:150] }}
{% endfor -%}
{%- endif %}
{%- set sniper = battle.get('sniper_points', {}) if battle else {} %}
{%- if sniper.ideal_buy or sniper.stop_loss or sniper.take_profit %}
{%- set ns = namespace(parts=[]) %}
{%- if sniper.ideal_buy %}{% set ns.parts = ns.parts + ['🎯' ~ labels.ideal_buy_label ~ ':' ~ (sniper.ideal_buy|string)[:15]] %}{% endif %}
{%- if sniper.stop_loss %}{% set ns.parts = ns.parts + ['🛑' ~ labels.stop_loss_label ~ ':' ~ (sniper.stop_loss|string)[:15]] %}{% endif %}
{%- if sniper.take_profit %}{% set ns.parts = ns.parts + ['🎊' ~ labels.take_profit_label ~ ':' ~ (sniper.take_profit|string)[:15]] %}{% endif %}
{{ ns.parts|join(' | ') }}
{%- endif %}
{%- set pos_advice = core.get('position_advice', {}) if core else {} %}
{%- if pos_advice.get('no_position') %}
🆕 {{ labels.no_position_label }}: {{ pos_advice.no_position[:200] }}
{%- endif %}
{%- if pos_advice.get('has_position') %}
💼 {{ labels.has_position_label }}: {{ pos_advice.has_position[:200] }}
{%- endif %}

---
{% endfor -%}
{%- endif %}

*{{ labels.report_time_label }}: {{ report_timestamp[11:16] }}*
{%- if show_llm_model and models_used %}
*{{ labels.analysis_model_label }}: {{ models_used|join(', ') }}*
{%- endif %}
